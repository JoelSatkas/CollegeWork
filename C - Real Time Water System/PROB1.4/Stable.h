struct Stable
{
	/*
		chlorinated: a boolean 'N/Y' that will indicate if the system is chlorinated.
		florinated: a boolean 'N/Y' that will indicate if the system is florinated.
	*/
	char chlorinated;
	char florinated;
};
#define STRUCT_SIZE_STABLE sizeof(struct Stable)